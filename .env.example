# ASR Backend Configuration
# Options: 'whisper' (local/offline development) or 'aws' (Amazon Transcribe Streaming)
ASR_BACKEND=whisper

# AWS Configuration (used when ASR_BACKEND=aws)
AWS_REGION=ap-south-1
AWS_TRANSCRIBE_LANGUAGE=en-IN
